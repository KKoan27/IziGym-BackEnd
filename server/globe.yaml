# globe.yaml na raiz do seu projeto

# Comando para iniciar sua aplicação APÓS o build
start: build/server

# Passos para construir sua aplicação
build:
  # 1. Baixa as dependências do Dart
  - dart pub get
  # 2. Compila seu arquivo principal para um executável
  - dart compile exe bin/server.dart -o build/server
